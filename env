# This file is sourced in env-setup.sh, and its values are branch specific
# See .gitattributes after making a clone.

# env-local can override. See that file.

# This can be used to activate any dev time behavor (as opposed to prod), and is also the
# default branch name when some automatec git clone and/or checkout is needed.
MENDS_ENV=dev
MENDS_ENVS_SHARED=/home/share1/mends-envs

WHISTLE_ROOT=
WHISTLE_REPO=https://source.developers.google.com/p/hdcekamends1/r/healthcare_data_harmonization
WHISTLE_ENV=mends-${MENDS_ENV}

MENDS_INPUT_ROOT=
MENDS_OUTPUT_ROOT=
